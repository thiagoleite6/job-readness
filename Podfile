# Uncomment the next line to define a global platform for your project
# platform :ios, '9.0'

target 'JobReadness' do
  # Comment the next line if you don't want to use dynamic frameworks
  use_frameworks!

  # Pods for JobReadness
  pod 'Kingfisher', '~> 7.0'

  target 'JobReadnessTests' do
    inherit! :search_paths
    # Pods for testing
  end

  target 'JobReadnessUITests' do
    # Pods for testing
  end

end
